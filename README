# Data Import Project for Firestore

This is a Node.js project that imports data from a CSV file into Firestore using the Firebase Admin SDK. It was developed with TypeScript and follows an MVC (Model-View-Controller) architecture.

## Settings

### Requirements

- Node.js installed
- Firebase account with a configured project
- Firebase service account key file (serviceAccountKey.json)
- Add the serviceAccountKey.json file to the (firebase) directory within the project
- CSV file with the data to be imported into Firestore

### Installation

Clone the repository:
git clone https://github.com/douglazfigueiredo/smash-gift-teste-nodejs.git

Navigate to the project directory:
cd smash-gift-teste-nodejs

Install project dependencies:
npm install

Compile TypeScript code to JavaScript:
npm run build

Make sure you have the CSV file with the data to be imported in the data directory.

Run the application:
npm start

This will start the process of importing the CSV data into Firestore.
